server:
  port: 9002

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Maddy23Git/micro-service-config
          clone-on-start: true
